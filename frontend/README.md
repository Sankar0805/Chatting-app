# React Frontend

This directory contains the React frontend for the chatting app.

Place your existing React source code under `src/` and public assets under `public/`.

Install dependencies with `npm install` or `yarn`, then run `npm run dev`.
